# Оптимизация под 6 GB RAM
shared_buffers = 1536MB
effective_cache_size = 4GB
maintenance_work_mem = 384MB
work_mem = 32MB

# Параллельные операции
max_worker_processes = 4
max_parallel_workers_per_gather = 2
max_parallel_workers = 4

# Журналирование
wal_level = minimal
max_wal_senders = 0 